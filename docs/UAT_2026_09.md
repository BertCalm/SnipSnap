# UAT pass, September 2026

A user-acceptance review of the app as it stands: ten simulated journeys
across three personas, run against the real `:shell` state machines, plus a
read of the Compose layer for the things a state machine can't show you —
what's hidden, what's unlabelled, and what a first-time user will never
find.

**Method, and its honest limit.** The Android app cannot be launched in
this environment (no SDK, no device), so nothing here is a screenshot.
What *is* real: every journey below was executed as code against the same
models `:app` binds to — `StarterKits`, `InstantKit`, `ChopReviewModel`,
`KitBuilderModel`, `ExportWizardModel`, `TapeDeckModel`, `PadSheet`,
`Preflight`, `Exporters`. Tap counts, timings, refusal wording, preflight
verdicts and format outputs are measurements, not estimates. Everything
about *layout* — what fits on a 390 dp frame, what a gesture looks like —
is read off the source and marked as such. The harness is
`shell/src/test/kotlin/com/snipsnap/shell/UatSimTest.kt`; it asserts
nothing and writes a full transcript to `uat-transcript.txt` in the JVM's
system temp directory (`java.io.tmpdir`).

```
./gradlew :shell:test --tests '*UatSimTest*'
```

---

## The headline

The engine room is in extraordinary shape. Sixteen pads of real DSP render
in 172 ms. Eight export formats all wrote clean files on the first pass.
Preflight blocks an empty kit and passes a good one. Refusals are specific
and in plain words — TUNE on a kick comes back *"only 1% of the sound lives
in its partials"*, which is better error copy than most shipping audio
software manages.

The problem is not the machine. It's that **the app never tells anyone
what it is.**

Three findings do most of the damage, and all three are cheap:

1. **HELP describes a product that no longer exists.** It says
   *"THIS IS THE M0 SKELETON… CAPTURE ARRIVES WITH M1."* Capture, chop,
   synth, groove, play and export have all shipped. The one screen a
   confused user opens tells them the app can't do the thing they just
   watched it do. The title bar still reads `M0` beside it.

2. **There is no first run.** No tour, no "here's the loop", no example.
   `Copy.BOOT_LINES` — the boot litany, already written — is never shown
   at boot; it only appears inside the stale HELP screen. A new user lands
   on an empty shelf holding one button and a joke.

3. **Roughly half the app's depth is behind one 480 ms hold that nothing
   on screen mentions.** PAD SHEET — 20 treatments, SHAPE, TUNE, MUTATE,
   LAYERS, TAKES, and the door to GRAIN FIELD — has no tap path and no menu
   entry. It is taught by a toast that fires on at most three kit-opens and
   then retires forever. The code already knows this: `PREF_PAD_SHEET_HINTS`
   in `App.kt` documents a prior audit reaching the same conclusion. The
   hint is a patch over a structural problem, not a fix.

---

## Findings, by severity

Severity is "what does this cost the user", not "how hard is it to fix".

### S1 — a new user is actively misled or stops here

| # | Finding | Evidence |
|---|---|---|
| **1** | **HELP documents the M0 skeleton and states that capture hasn't shipped.** It is the only in-app explanation of anything, and every line of it is out of date. | `ui/StubScreen.kt:53` — `HelpScreen()` is 20 lines, hardcoded, last true at M0 |
| **2** | **Title bar build tag reads `M0`.** Next to a stale HELP this reads as an abandoned prototype. | `ui/Chrome.kt` `TitleBar()` |
| **3** | **No onboarding of any kind.** The product loop (capture → trim → grid → export) is never stated anywhere in the app. | no first-run branch exists in `App.kt`; `Copy.BOOT_LINES` has no caller outside HELP |
| **4** | **PAD SHEET is unreachable without knowing a secret gesture.** 20 treatment segments + shape/tune/mutate/layers/takes/grain behind one unhinted 480 ms hold on a filled pad. | `App.kt` `onLongPress`; `KitScreen.kt` `PadCell`; `PadSheet.ALL_SEGMENTS.size == 20` |
| **5** | **The hint that teaches it is capped at 3 showings, then never returns.** A user who dismisses it three times while learning something else loses that half of the app permanently. | `PAD_SHEET_HINT_LIMIT = 3`, `PAD_SHEET_FOUND = -1` in `App.kt` |

### S2 — friction that makes the tool slower or smaller than it is

| # | Finding | Evidence |
|---|---|---|
| **6** | **Correcting a chop label is a 10-state one-way cycle.** Tap cycles KICK → SNARE → CLAP → HAT_CLOSED → HAT_OPEN → TOM → PERC → TONAL → LOOP → UNKNOWN. Overshoot and you go round again. Long-press only resets to the machine's guess — it isn't a back step. Worst case to relabel a 16-slice kit: **144 taps.** | measured; `ChopReviewModel.CHIP_CYCLE`, `ui/ChopScreen.kt:450` |
| **7** | **Export format is an 8-state one-way cycle with no back step and no memory.** Reaching DECENTSAMPLER is 7 taps; one tap past your target costs 7 more. Every visit to EXPORT starts at index 0, even if you exported `.xtd` five times running. | measured; `ExportWizardModel.cycleFormat`, `ui/ExportScreen.kt:498` |
| **8** | **The format cycler label is the only explanation a format ever gets.** Nothing says when you'd pick EXPANSION over XPN, or SFZ over DECENTSAMPLER. | `ExportFormat.cyclerLabel` is the sole copy |
| **9** | **The menu row is 26 dp tall** — in a `Layout` object that also declares `MIN_HIT_TARGET = 44`. It's the app's primary navigation and its smallest touch target. | `Layout.MENU_ROW_H = 26` vs `Layout.MIN_HIT_TARGET = 44` |
| **10** | **The menu row scrolls horizontally with no arrow, fade or overflow cue.** At the 390 dp design width the 11 tabs need ~400 dp; **SETUP and HELP sit off the right edge** with nothing indicating they exist. On a 360 dp phone EXPORT joins them. | measured against `Layout.FRAME_W`/`OUTER_MARGIN`; `ui/Chrome.kt` `horizontalScroll` |
| **11** | **Bank B pads can never be edited.** EVIL TWINS writes 16 pads to slots 17–32. `KitScreen`'s grid is `GRID_ROWS = 1..16`, and PAD SHEET is only reachable from that grid. They're playable on PLAY's landscape view and they export correctly — but they cannot be inspected, treated, tuned, renamed or cleared, ever. | `KitScreen.kt:76`; harness: `remixBankB` → slots 17..32 |
| **12** | ~~**SNIPS → PAD on an empty shelf is a dead end.**~~ **Corrected 2026-09-10 — this finding was wrong, and the smaller real gap is fixed.** It is not a dead end: `NEW KIT ▸ STARTERS` sits enabled right below the panel, exactly where `EMPTY_SHELF_FOR_ASSIGN` says it does, and `fresh()` never clears `pendingSnipAssign`, so the hand-off survives being sent to make a kit — the new kit opens with the snip still armed. What was actually missing: `fresh()` said only FRESH_TAPE, so on the one route that copy points at, the user landed on a new kit with no instruction, while every other way in says what to do next. Both call sites now share `Copy.snipLanding`. | read wrong in the first pass: the finding cited `pendingSnipAssign` without following `fresh()`, which is the function that decides |
| **13** | ~~**There is no un-treat.**~~ **Fixed 2026-09-10.** The finding was right: the TREATMENT card's NONE chip was display-only, and undoing a treatment meant leaving PAD SHEET for TAKES + BIN and restoring a whole-kit take, rolling back everything else done since. NONE takes the tap now. The move was already in the model — `unEraPad`, the same door `applyTreatment` uses to get back to clean audio before stacking a new treatment; what was missing was a way to ask for it. `PadSheet.unTreatState` gives the four honest answers (nothing to undo, ready, the ghosts postdate the treatment, the bin never held the take) and the card speaks whichever it gets. | was `ui/PadSheetScreen.kt:1849`; now `PadSheet.unTreatState` + `PadSheetScreen.unTreat()` |
| **14** | **A slow treatment looks like a frozen screen.** ETERNAL took **1.77 s** on a desktop JVM (SKIM 480 ms, DUB 247 ms, GHOST 162 ms); a phone will be several times slower. While `busy` is true the chips silently stop accepting taps — no label change, no spinner, no dimming. The user taps ETERNAL, nothing happens, taps again, still nothing. | measured; `TreatmentCard`'s `tappable = !busy && …` with no visual busy state |

### S3 — trust, polish, and things that get worse with use

| # | Finding | Evidence |
|---|---|---|
| **15** | ~~**Every kit reads DRAFT forever.**~~ **Fixed 2026-09-10.** Nothing anywhere recorded a dub — `ExportOutcome` lives only while the completion screen is mounted — so the chip had nothing to read. `DubStamp` writes a sidecar into the kit's own folder when EXPORT completes; the shelf reads it against the card the phone is holding. **Three states, because three are honestly distinguishable:** DRAFT (never written out), DUBBED (written, but not to this card), ON CARD (written to the card in the phone now). The middle one exists so neither lie is needed — calling it DRAFT denies work the user did, calling it ON CARD promises a card that isn't there. A sidecar rather than a preference because `renameKit` *moves the directory*; rather than `kit.json` because which card someone's phone wrote to is not part of what a kit is. | was `ui/KitsScreen.kt`, hardcoded `"DRAFT"`; now `DubStamp` + `Copy.dubChip` |
| **16** | **The shelf has no sort, filter, search or date.** One flat list in `KitStore` order; a row shows name, pad count, tempo, DRAFT. At 20+ kits the only way to find one is to scroll and read. | `KitsScreen` `LazyColumn` over `kits` as given |
| **17** | ~~**Naming a kit takes three gestures and a secret.**~~ **The secret is fixed 2026-09-10; the "ask once, up front" half is a design call and is not.** The shelf now carries a permanent legend — *HOLD A KIT TO RENAME OR DELETE IT* — in the same words as the row's own TalkBack long-press label, so the sighted and the spoken app agree. It is the third of these (ROOMS and KIT already had one), which also advances finding 21. Auto-naming at every creation door is unchanged: prompting for a name up front is a product decision, not a defect, and it belongs to whoever owns the creation flow. | was `KitShelf.freshName`; `KitRow`'s `onLongClick` — now `Copy.SHELF_LEGEND` under the shelf list |
| **18** | ~~**Re-exporting to the same destination silently overwrites.**~~ **Fixed 2026-09-10.** DUB now writes with `overwrite = false` on the first tap. If something is already there the write does not happen: the toast names the exact thing in the way and says *DUB AGAIN TO WRITE OVER IT*, and only that second tap passes `overwrite = true`. Changing format, or a preflight block, disarms it — a confirmation taken against one destination is not consent for another. The enabling change is a typed `DestinationExists` refusal in `:kit`, replacing thirteen hand-written copies of the same `IOException` message: without a type, "there is already a kit here" could not be told from "the card filled up" except by matching on prose, and treating a full card as a confirmable overwrite would be worse than the bug. | was `ui/ExportScreen.kt:359`; now `WriteResult.WouldOverwrite` + `ExportSession.overwriting` |
| **19** | ~~**COMMIT and INSTANT KIT disagree about an empty selection.**~~ **Fixed 2026-09-10.** The finding was right that the user was never told which they got. The fix is not to make INSTANT KIT refuse — chopping the whole tape is the right default for a one-tap button, and the silence about it was the bug. The toast names the scope now: *ONE TAP, THE WHOLE TAPE* or *ONE TAP, YOUR IN + OUT*, borrowing COMMIT's own two markers so the pair of buttons speaks one vocabulary. TAPE passes the answer down rather than letting it be inferred, since an IN/OUT dragged across the whole tape reaches `App.kt` looking identical to no selection at all. | was `ui/TapeScreen.kt:680` vs `:702`; now `Copy.instantKit(…, wholeTape)` |
| **20** | ~~**SMEAR and TAIL are on adjacent rows of the same card…**~~ **Fixed 2026-09-10 — and the special-casing was leaking, not just confusing.** The naming half was already resolved (the character draws as TAIL) but nothing stopped the two being wired back together, so the separation is pinned: SMEAR dispatches through no era/character/keyed door, `"smeared"` belongs to TAIL alone, and the two can never share a label. The engine name can't be renamed to break the tie — `"smeared"` is written into recipes on disk. The real cost was elsewhere: each family's restore-first guard only recognised **its own** recipe shape, so CRUSH on a smeared pad baked the era onto the stretched audio and then lit CRUSH alone, and SMEAR on an aged pad did the mirror. Both guards now ask `PadSheet.unTreatState`, which reads both shapes. Proof is byte-stable: smearing an aged pad produces exactly what smearing the original produces. | was `PadSheet.kt:35`; now `unTreatState` in `smearPad` and `applyTreatment` |
| **21** | **Partly fixed 2026-09-10 — three of five now have a legend.** ROOMS always did; PAD SHEET got `Copy.PAD_SHEET_LEGEND` (findings 4/5) and the kit shelf got `Copy.SHELF_LEGEND` (finding 17). Still silent: chop RECLASSIFY and the EXPORT card's forget-this-card. Also fixed here, from the same family: the EXPORT format rows passed an explicit `tapeClick` label over **two** `TapeText` children, which replaces merged descendant semantics — so a TalkBack user heard the format name and never the reason line finding 8 added. | grep across `:app`: `onLongClickLabel`s are TalkBack-only; visible legends now 3 of 5 |
| **22** | ~~**Scheme count has drifted from the docs.**~~ **Fixed 2026-09-10, and it was worse than a count.** The README's "six TapeOS scheme token tables" is now eight. But chasing the number found that `docs/UI_DESIGN.md` names *six different schemes* — Chrome, Ferric, Metal, Snack Bar, Oilslick, Clear — of which only **two** survive; `design/Schemes.dc.html` matches the code exactly, so that section is marked superseded and points at the artboard rather than being rewritten from guesses. The same sweep found `app/README.md` a starter count out (six vs eight) and a `LoopGrid.kt` comment deferring to the superseded table. A `ConventionTest` law now ties every written scheme/starter count to `SchemeId.entries.size` / `StarterKits.ALL.size`. | was `SchemeId.entries.size == 8` vs the prose; now pinned by the roster-count law |
| **23** | **SETUP holds three settings** — SCHEME, PERSONALITY, TEACH THE MACHINE. No default export format, no "where do my files live", no capture defaults, no link to HELP. | `ui/PropertiesScreen.kt` |

---

## Journey notes

### J1 · New user, cold start → a kit on the MPC — **6 taps**

The best path in the app, and it works.

```
[1] shelf: "NOTHING TAPED YET. GO STEAL A SOUND (LEGALLY)."
[2] NEW KIT ▸ STARTERS  →  PICK A STARTER (8 entries, each with a blurb)
[3] FACTORY             →  DUBBING… 172 ms, 16 pads, auto-opens KIT
[4] EXPORT              →  preflight 3× OK, 17 files queued
[5] DESTINATION         →  system folder picker
[6] WRITE KIT           →  FACTORY.xpm
```

The starter menu is the single best-designed thing in the app: eight
options, each with a one-sentence blurb that predicts the result, and
EMPTY GRID sitting first and honestly named. `NEW KIT ▸ STARTERS` is a
good label — it predicts a menu, not a recording.

Picking EMPTY GRID instead is the one cold-start cul-de-sac: 0 pads,
`"16 EMPTY PADS. TERRIFYING."`, and the only instruction is a toast you
get by tapping a pad — LONG-PRESS TO CAPTURE. Preflight correctly refuses
to export it (`[FAIL] no pads assigned — nothing to export`), which is the
right call, arrived at three screens too late.

### J2 · Capture something in the room — **5 taps on the fast path**

ARM → SNIP works, the armed UI is well judged (level meter *and* a ticking
mm:ss, so a silent room can't be misread as a dead app), and the optimistic
SNIP toast is corrected if the commit actually fails. Good.

But **the snip goes somewhere the user isn't**. After SNIP, nothing
navigates. The snip is in SNIPS, which is a button near the bottom of the
shelf below BACKUP. The path the app clearly wants — TAPE → INSTANT KIT,
which produced a playable 4-pad kit in 95 ms — is not the path the SNIP
button points at, because it points at nothing.

### J3 · Arguing with the classifier

Classification was confident and correct on the test material (KICK 0.91,
SNARE 0.88/0.95, CLAP 0.70) and the NOT SURE dashed chip is exactly the
right piece of honesty. The placement summary line — `AUTO-PLACE: KICK→A01
SNARE→A02` — is genuinely good copy.

The cost is finding #6: a 10-state one-way cycle is the wrong control for
a 10-value choice. A grid of ten chips in a sheet would be one tap.

### J4 · Export fan-out — all 8 formats wrote clean

| format | landed |
|---|---|
| MPC 2 FOLDER | `FANOUT.xpm` |
| EXPANSION | `FANOUT/` |
| XPN ARCHIVE | `FANOUT.xpn` |
| MPC 3 NATIVE | `FANOUT.xtd` |
| MPC SESSION | `FANOUT.xpj` |
| MIDI GROOVES | `FANOUT Preview.mid` |
| SFZ | `FANOUT.sfz` |
| DECENTSAMPLER | `FANOUT.dspreset` |

Zero failures, zero exceptions. The pipeline is the strongest part of the
product and the wizard around it is the weakest-supported: eight one-way
taps and no explanation of what any of them mean.

### J5 · The depth behind the hold

Every one of the 20 segments applied successfully on a real kick sample:

```
CRUSH → sp1200 18ms    TAIL  → smeared 102ms   GHOST → ghosted 162ms
TAPE  → tape   13ms    SLAP  → slapback 12ms   STOP  → stopped  17ms
DIRT  → mpc60   7ms    WASH  → washed   34ms   START → started  19ms
SKIM  → skimmed 480ms  PUNCH → punched  41ms   FLIP  → reversed 32ms
DUB   → dubbed  247ms  BODY  → bodied  114ms   SWELL → swelled  59ms
WOBBLE→ wobbled  46ms  ETERNAL→ eternal 1773ms
TUNE  → refused: "only 1% of the sound lives in its partials"
```

That refusal is a highlight. It declined to retune a kick, and it said why
in language a musician understands. That's the standard the rest of the
app's copy should be held to — and mostly is.

### J6 · Day 5, a shelf with history

Seven kits in, the shelf is already thin: no sort, no search, no dates, no
"on card" state, no art. Every row is `NAME · N PADS · DRAFT`. This is the
screen that degrades fastest with success — a user who likes the app will
have 40 kits by month two and no way to navigate them.

### J7 · Edges — handled well

Silence chops to 0 slices and INSTANT KIT refuses with a *useful* message
(*"no hits found to chop — trim closer to the sound, or use CHOP's grid"* —
it names the alternative). Name validation is correct and its refusal
copy spells out the forbidden characters. Marking OUT before IN swaps them
rather than erroring. The export stage machine refuses illegal transitions.

Two gaps: silent overwrite (#18), and `Ünïcödé` is refused with no
transliteration offered — reasonable for MPC safety, but a user with a
non-ASCII keyboard hits a wall the copy doesn't help them past.

### J8 · Power-user ceiling

EVIL TWINS lit 16 bank-B pads in 118 ms; ghost layers built 3 velocity
zones; IN KEY moved exactly the 4 tonal pads and left the drums alone
(right call, and it says so); the 35-file deep kit exported clean.

Two ceilings: bank B is uneditable (#11), and DESAMPLE on a
velocity-layered pad refuses with *"clear the layers before rewriting its
audio"* — correct and clear, but there's no "clear the layers" button
offered next to the refusal.

### J9 · The trim deck

Physics ported from the prototype; snap-to-onset and snap-to-zero both on
by default, which is the right default. Selection snapped cleanly to
onsets. Zoom is a 3-state one-way cycler (x1 → x2 → x4) with no pinch —
mild, but it's the third one-way cycler in the app.

### J10 · The frame

Covered in findings #1, #2, #9, #10, #22, #23.

One note on PERSONALITY: at OFF, toasts are drawn at alpha 0. The
accessibility reasoning in `Chrome.kt` is careful and correct — the live
region is still composed for TalkBack. But a *sighted* user at OFF loses
every confirmation the app gives: no "kit deleted", no "shared", no
"renamed". OFF is presented as a tone setting; it currently doubles as a
feedback setting.

---

## What's working, and shouldn't be touched

- **The starter menu.** Eight options, honest blurbs, EMPTY GRID first and
  correctly named. Cold start is solved.
- **Refusal copy.** Nearly every refusal names the cause and often the fix.
  The TUNE and INSTANT KIT refusals are better than most commercial audio
  software.
- **Preflight.** Blocks what's broken, passes what isn't, shows its work as
  a checklist. Exactly right for a tool that writes to someone's SD card.
- **The armed-capture indicator.** Level meter plus wall-clock counter,
  anchored to a real timestamp so a remount doesn't lie about session
  length. That's a detail most apps get wrong.
- **The bins.** Pads, rooms and kits all get a 30-day bin with visible days
  remaining and a restore. Destructive actions are genuinely reversible.
- **The accessibility pass.** The September audit's fixes are real:
  `tapeClick` forces an explicit label decision at every call site, and
  the KDoc explaining *why* a label is null is better documentation than
  most codebases have for the accessible path.
- **Performance.** 172 ms for a 16-pad kit, 95 ms for a live chop. The
  thin-shell-over-proven-code bet paid off.

---

## If only three things get fixed

1. **Rewrite HELP and drop the `M0` tag** (hours). It is the cheapest
   possible fix and the app currently misrepresents itself to every user
   who looks for guidance.
2. **Give PAD SHEET a visible door** (a day). A `PAD ▸` chip in KIT's
   action row, or a persistent one-line legend under the grid the way
   ROOMS already has one. The long-press can stay as the fast path.
3. **Turn the two one-way cyclers into pickers** (a day). Ten class chips
   in a sheet; eight format rows with a line of explanation each. This is
   the difference between a tool that feels fast and one that feels like
   it's making you count.

---

*Findings 1–23 are reproducible: 15 of them are emitted by the harness
transcript, the rest are cited to file and line above.*
