# AndroidX is required by any module using the Android Gradle Plugin —
# :app pulls in androidx.* transitively via Compose, and AGP refuses to
# build without this flag set. Not needed by the eight pure-JVM modules,
# but harmless for them since none declare Android dependencies.
android.useAndroidX=true
